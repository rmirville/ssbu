import { TestBed } from '@angular/core/testing';

import { StageLoaderConstService } from './stage-loader-const.service';

describe('StageLoaderConstService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));
  describe('loadStages', () => {
  });
});
